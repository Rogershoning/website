---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Contact & Bestellen">
	<main class="max-w-5xl mx-auto p-4 md:p-12 text-stone-700">
		<div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-start">
			
			<div class="space-y-8">
				<header>
					<h1 class="text-5xl md:text-6xl font-bold mb-6 italic leading-tight text-[#2D2D2D]">
						Neem <span class="text-[#FFB400]">Contact</span> op
					</h1>
					<p class="text-xl text-stone-500 italic font-light leading-relaxed">
						Voor het bestellen van honing uit Malden, advies over biodiversiteit of vragen over maatwerk 3D-projecten en lasersnijden.
					</p>
				</header>

				<div class="space-y-4">
					<div class="bg-white p-6 rounded-[2rem] border border-stone-100 shadow-sm flex items-center gap-4 transition-transform hover:scale-[1.02]">
						<div class="bg-amber-50 h-12 w-12 rounded-full flex items-center justify-center text-xl text-amber-600">📧</div>
						<div>
							<p class="text-[10px] font-black uppercase tracking-widest text-stone-400">E-mail</p>
							<p class="font-bold text-[#2D2D2D]">roger@rogershoning.nl</p>
						</div>
					</div>
					
					<div class="bg-blue-50 p-6 rounded-[2rem] border border-blue-100 shadow-sm flex items-center gap-4 transition-transform hover:scale-[1.02]">
						<div class="bg-white h-12 w-12 rounded-full flex items-center justify-center text-xl">📍</div>
						<div>
							<p class="text-[10px] font-black uppercase tracking-widest text-blue-400">Locatie imkerij</p>
							<p class="font-bold text-[#2D2D2D]">Malden, Nederland</p>
						</div>
					</div>
				</div>

				<div class="p-8 bg-[#2D2D2D] rounded-[2.5rem] text-white relative overflow-hidden shadow-xl">
					<div class="relative z-10">
						<h3 class="text-xl font-bold mb-2 text-[#FFB400]">Duurzaamheid</h3>
						<p class="text-sm text-stone-300 leading-relaxed italic">
							Inruilen loont: € 1,50 korting bij inlevering van het gegraveerde zeshoekige glas.
						</p>
					</div>
					<div class="absolute -right-4 -bottom-4 text-8xl opacity-10 italic">🍯</div>
				</div>
			</div>

			<!-- Formulier sectie -->
			<div class="bg-white p-8 md:p-12 rounded-[3rem] shadow-2xl border border-stone-100">
				<form 
					action="https://formspree.io" 
					method="POST" 
					class="space-y-5"
				>
					<input type="hidden" name="_subject" value="Nieuwe bestelling/vraag via RogersHoning.nl" />
					
					<div>
						<label class="block text-[10px] font-black uppercase mb-2 ml-1 tracking-widest text-stone-400">Naam</label>
						<input type="text" name="name" required class="w-full bg-stone-50 border-none rounded-2xl p-4 outline-none focus:ring-2 focus:ring-[#FFB400] transition-all shadow-inner" />
					</div>

					<div>
						<label class="block text-[10px] font-black uppercase mb-2 ml-1 tracking-widest text-stone-400">E-mailadres</label>
						<input type="email" name="_replyto" required class="w-full bg-stone-50 border-none rounded-2xl p-4 outline-none focus:ring-2 focus:ring-[#FFB400] transition-all shadow-inner" />
					</div>

					<div>
						<label class="block text-[10px] font-black uppercase mb-2 ml-1 tracking-widest text-stone-400">Bericht</label>
						<textarea name="message" rows="5" required class="w-full bg-stone-50 border-none rounded-2xl p-4 outline-none focus:ring-2 focus:ring-[#FFB400] transition-all resize-none shadow-inner" placeholder="Wat kan ik voor je betekenen?"></textarea>
					</div>

					<button type="submit" class="w-full bg-[#2D2D2D] text-white py-5 rounded-2xl font-black uppercase text-xs tracking-[0.2em] hover:bg-[#FFB400] hover:text-[#2D2D2D] transition-all shadow-xl active:scale-95">
						Verstuur Bericht
					</button>
				</form>
			</div>
		</div>
	</main>
</MainLayout>

<script>
	// Forceert een normale browser-reload voor dit formulier om ViewTransition-errors te omzeilen
	document.addEventListener('astro:page-load', () => {
		const form = document.querySelector('form');
		if (form) {
			form.addEventListener('submit', () => {
				// Geen extra JS nodig, de browser doet het werk
			});
		}
	});
</script>
